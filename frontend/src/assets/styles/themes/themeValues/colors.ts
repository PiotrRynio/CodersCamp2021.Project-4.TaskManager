import { gold } from '@ant-design/colors';

export type Colors = {
  primary: string;
  regularText: string;
  gold: string;
  gray: string;
  transparent: string;
};

export const colors: Colors = {
  primary: '#4eadc5',
  regularText: '#000',
  gold: gold[5],
  gray: '#eee',
  transparent: 'transparent',
};
